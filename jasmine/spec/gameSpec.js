describe('should create a class', function() {
  it('should be named Game', function() {
    expect(typeof Game).toEqual('function');
  });

  it('should declare a constructor function', function() {
    expect(typeof constructor).toEqual('function');
  });
});
